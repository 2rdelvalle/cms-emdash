diff --git a/src/api/public-url.ts b/src/api/public-url.ts
index cb5afb9b7595c518fef51fd639ede8cadfc84632..938ad7fa48a821601a88b650bdd0d493908a0ab9 100644
--- a/src/api/public-url.ts
+++ b/src/api/public-url.ts	
@@ -82,3 +82,18 @@ export function getPublicOrigin(url: URL, config?: EmDashConfig): string {
 export function getPublicUrl(url: URL, config: EmDashConfig | undefined, path: string): string {
 	return `${getPublicOrigin(url, config)}${path}`;
 }
+
+export function getForwardedRequestUrl(request: Request): URL {
+	const reqUrl = new URL(request.url);
+
+	const forwardedProto =
+		request.headers.get("x-forwarded-proto")?.split(",")[0]?.trim() ||
+		reqUrl.protocol.replace(":", "");
+
+	const forwardedHost =
+		request.headers.get("x-forwarded-host")?.split(",")[0]?.trim() ||
+		request.headers.get("host")?.split(",")[0]?.trim() ||
+		reqUrl.host;
+
+	return new URL(`${forwardedProto}://${forwardedHost}${reqUrl.pathname}${reqUrl.search}`);
+}
diff --git a/src/astro/routes/admin.astro b/src/astro/routes/admin.astro
index 9760ea43cae9f0e7d60cbbfec94d0f9c901440e7..9201276bee77a3e31389c07020b2ec590928ecb7 100644
--- a/src/astro/routes/admin.astro
+++ b/src/astro/routes/admin.astro	
@@ -27,7 +27,286 @@ const messages = await loadMessages(resolvedLocale);
 			rel="icon"
 			href="data:image/svg+xml,<svg width='75' height='75' viewBox='0 0 75 75' fill='none' xmlns='http://www.w3.org/2000/svg'> <g clip-path='url(%23clip0_50_99)'> <rect x='3' y='3' width='69' height='69' rx='10.518' stroke='url(%23paint0_linear_50_99)' stroke-width='6'/> <rect x='18' y='34' width='39.3661' height='6.56101' fill='url(%23paint1_linear_50_99)'/> </g> <defs> <linearGradient id='paint0_linear_50_99' x1='-42.9996' y1='124' x2='92.4233' y2='-41.7456' gradientUnits='userSpaceOnUse'> <stop stop-color='%230F006B'/> <stop offset='0.0833333' stop-color='%23281A81'/> <stop offset='0.166667' stop-color='%235D0C83'/> <stop offset='0.25' stop-color='%23911475'/> <stop offset='0.333333' stop-color='%23CE2F55'/> <stop offset='0.416667' stop-color='%23FF6633'/> <stop offset='0.5' stop-color='%23F6821F'/> <stop offset='0.583333' stop-color='%23FBAD41'/> <stop offset='0.666667' stop-color='%23FFCD89'/> <stop offset='0.75' stop-color='%23FFE9CB'/> <stop offset='0.833333' stop-color='%23FFF7EC'/> <stop offset='0.916667' stop-color='%23FFF8EE'/> <stop offset='1' stop-color='white'/> </linearGradient> <linearGradient id='paint1_linear_50_99' x1='91.4992' y1='27.4982' x2='28.1217' y2='54.1775' gradientUnits='userSpaceOnUse'> <stop stop-color='white'/> <stop offset='0.129253' stop-color='%23FFF8EE'/> <stop offset='0.617058' stop-color='%23FBAD41'/> <stop offset='0.848019' stop-color='%23F6821F'/> <stop offset='1' stop-color='%23FF6633'/> </linearGradient> <clipPath id='clip0_50_99'> <rect width='75' height='75' fill='white'/> </clipPath> </defs> </svg>"
 		/>
-		<title>EmDash Admin</title>
+		<title>VoiceToPhone CMS</title>
+		<script is:inline>
+			(() => {
+				const routeKey = "data-emdash-route";
+				const loginMatcher = /\/_emdash\/admin\/login\/?$/;
+				const syncRoute = () => {
+					document.documentElement.setAttribute(
+						routeKey,
+						loginMatcher.test(window.location.pathname) ? "login" : "app",
+					);
+				};
+
+				syncRoute();
+
+				const wrapHistoryMethod = (method) => {
+					const original = window.history[method];
+					window.history[method] = function (...args) {
+						const result = original.apply(this, args);
+						queueMicrotask(syncRoute);
+						return result;
+					};
+				};
+
+				wrapHistoryMethod("pushState");
+				wrapHistoryMethod("replaceState");
+				window.addEventListener("popstate", syncRoute);
+			})();
+		</script>
+		<style is:inline>
+			:root {
+				--vtp-login-bg: #f4f8fc;
+				--vtp-login-surface: rgba(255, 255, 255, 0.92);
+				--vtp-login-surface-strong: #ffffff;
+				--vtp-login-text: #0f2740;
+				--vtp-login-muted: #5e7388;
+				--vtp-login-border: rgba(14, 63, 112, 0.12);
+				--vtp-login-primary: #0f6cbd;
+				--vtp-login-primary-hover: #0b5ba3;
+				--vtp-login-primary-soft: rgba(15, 108, 189, 0.12);
+				--vtp-login-accent: #19a7ce;
+				--vtp-login-shadow: 0 28px 60px rgba(15, 39, 64, 0.16);
+			}
+
+			html[data-emdash-route="login"] {
+				background:
+					radial-gradient(circle at top left, rgba(25, 167, 206, 0.16), transparent 34%),
+					radial-gradient(circle at bottom right, rgba(15, 108, 189, 0.18), transparent 30%),
+					linear-gradient(180deg, #f7fbff 0%, #eef4fa 100%);
+			}
+
+			html[data-emdash-route="login"] body {
+				min-height: 100vh;
+				margin: 0;
+				background: transparent;
+				color: var(--vtp-login-text);
+				font-family:
+					"IBM Plex Sans",
+					"Segoe UI",
+					"Helvetica Neue",
+					Arial,
+					sans-serif;
+			}
+
+			html[data-emdash-route="login"] body::before {
+				content: "";
+				position: fixed;
+				inset: 0;
+				pointer-events: none;
+				background-image:
+					linear-gradient(rgba(15, 108, 189, 0.045) 1px, transparent 1px),
+					linear-gradient(90deg, rgba(15, 108, 189, 0.045) 1px, transparent 1px);
+				background-size: 72px 72px;
+				mask-image: linear-gradient(180deg, rgba(0, 0, 0, 0.32), transparent 82%);
+			}
+
+			html[data-emdash-route="login"] #admin-root {
+				position: relative;
+			}
+
+			html[data-emdash-route="login"] #admin-root::before {
+				content: "";
+				position: fixed;
+				inset: auto auto 12vh 8vw;
+				width: min(28vw, 22rem);
+				height: min(28vw, 22rem);
+				border-radius: 999px;
+				background: radial-gradient(circle, rgba(25, 167, 206, 0.18), transparent 72%);
+				filter: blur(6px);
+				pointer-events: none;
+			}
+
+			html[data-emdash-route="login"] #admin-root > div.min-h-screen.flex.items-center.justify-center.bg-kumo-base.p-4 {
+				position: relative;
+				min-height: 100vh;
+				padding: 2rem;
+				background: transparent;
+			}
+
+			html[data-emdash-route="login"] #admin-root > div.min-h-screen.flex.items-center.justify-center.bg-kumo-base.p-4 > div.w-full.max-w-md {
+				max-width: 29rem;
+			}
+
+			html[data-emdash-route="login"] #admin-root > div.min-h-screen.flex.items-center.justify-center.bg-kumo-base.p-4 > div.text-center {
+				position: relative;
+				min-width: min(90vw, 26rem);
+				padding-top: 0.5rem;
+			}
+
+			html[data-emdash-route="login"] #admin-root > div.min-h-screen.flex.items-center.justify-center.bg-kumo-base.p-4 > div.text-center::before {
+				content: "VoiceToPhone CMS";
+				display: block;
+				font-size: clamp(2rem, 4vw, 2.5rem);
+				font-weight: 600;
+				line-height: 1.05;
+				letter-spacing: -0.05em;
+				color: var(--vtp-login-text);
+				margin-bottom: 1rem;
+			}
+
+			html[data-emdash-route="login"] #admin-root > div.min-h-screen.flex.items-center.justify-center.bg-kumo-base.p-4 > div.text-center > svg[aria-label="EmDash"] {
+				display: none;
+			}
+
+			html[data-emdash-route="login"] .text-center.mb-8 {
+				position: relative;
+				margin-bottom: 1.75rem;
+				padding-top: 0.5rem;
+			}
+
+			html[data-emdash-route="login"] .text-center.mb-8::before {
+				content: "VoiceToPhone CMS";
+				display: block;
+				font-size: clamp(2rem, 4vw, 2.5rem);
+				font-weight: 600;
+				line-height: 1.05;
+				letter-spacing: -0.05em;
+				color: var(--vtp-login-text);
+			}
+
+			html[data-emdash-route="login"] .text-center.mb-8 svg[aria-label="EmDash"],
+			html[data-emdash-route="login"] .text-center.mb-8 img {
+				display: none;
+				filter: saturate(0.92) contrast(1.04);
+			}
+
+			html[data-emdash-route="login"] h1.text-2xl.font-semibold.text-kumo-default {
+				display: none;
+			}
+
+			html[data-emdash-route="login"] .mb-6.rounded-lg.bg-kumo-danger\\/10.border.border-kumo-danger\\/20.p-4.text-sm.text-kumo-danger {
+				border-color: rgba(186, 63, 63, 0.22);
+				background: rgba(255, 244, 244, 0.88);
+				backdrop-filter: blur(8px);
+			}
+
+			html[data-emdash-route="login"] .bg-kumo-base.border.rounded-lg.shadow-sm.p-6 {
+				position: relative;
+				overflow: hidden;
+				padding: 1.8rem;
+				border: 1px solid var(--vtp-login-border);
+				border-radius: 1.5rem;
+				background:
+					linear-gradient(180deg, rgba(255, 255, 255, 0.96) 0%, rgba(247, 250, 253, 0.92) 100%);
+				box-shadow: var(--vtp-login-shadow);
+				backdrop-filter: blur(18px);
+			}
+
+			html[data-emdash-route="login"] .bg-kumo-base.border.rounded-lg.shadow-sm.p-6::before {
+				content: "";
+				position: absolute;
+				inset: 0;
+				background: linear-gradient(135deg, rgba(15, 108, 189, 0.14), transparent 24%, transparent 100%);
+				pointer-events: none;
+			}
+
+			html[data-emdash-route="login"] .space-y-6 > * + * {
+				margin-top: 1.1rem;
+			}
+
+			html[data-emdash-route="login"] button,
+			html[data-emdash-route="login"] a[role="button"] {
+				transition:
+					transform 160ms ease,
+					box-shadow 160ms ease,
+					background-color 160ms ease,
+					border-color 160ms ease;
+			}
+
+			html[data-emdash-route="login"] button:hover,
+			html[data-emdash-route="login"] a[role="button"]:hover {
+				transform: translateY(-1px);
+			}
+
+			html[data-emdash-route="login"] button.w-full.justify-center:not(.variant-ghost),
+			html[data-emdash-route="login"] .space-y-6 > button:first-child {
+				min-height: 3rem;
+				border-radius: 0.95rem;
+			}
+
+			html[data-emdash-route="login"] .space-y-6 > button:first-child {
+				background: linear-gradient(135deg, var(--vtp-login-primary) 0%, #1781d6 100%);
+				box-shadow: 0 14px 30px rgba(15, 108, 189, 0.28);
+			}
+
+			html[data-emdash-route="login"] .space-y-6 > button:first-child:hover {
+				background: linear-gradient(135deg, var(--vtp-login-primary-hover) 0%, #116fbf 100%);
+				box-shadow: 0 18px 34px rgba(15, 108, 189, 0.32);
+			}
+
+			html[data-emdash-route="login"] .grid.grid-cols-2.gap-3 > button {
+				min-height: 3rem;
+				border-radius: 0.95rem;
+				border-color: rgba(15, 108, 189, 0.16);
+				background: rgba(255, 255, 255, 0.82);
+				color: var(--vtp-login-text);
+			}
+
+			html[data-emdash-route="login"] .grid.grid-cols-2.gap-3 > button:hover {
+				border-color: rgba(15, 108, 189, 0.32);
+				background: rgba(255, 255, 255, 0.98);
+				box-shadow: 0 10px 24px rgba(15, 39, 64, 0.08);
+			}
+
+			html[data-emdash-route="login"] .relative.flex.justify-center.text-xs.uppercase span {
+				border-radius: 999px;
+				background: rgba(244, 248, 252, 0.96);
+				color: var(--vtp-login-muted);
+				letter-spacing: 0.12em;
+			}
+
+			html[data-emdash-route="login"] .w-full.justify-center {
+				font-weight: 600;
+			}
+
+			html[data-emdash-route="login"] .text-center.mt-6.text-sm.text-kumo-subtle,
+			html[data-emdash-route="login"] .mt-6.flex.justify-center.gap-2.text-xs.text-kumo-subtle {
+				color: var(--vtp-login-muted);
+			}
+
+			html[data-emdash-route="login"] .mt-6.flex.justify-center.gap-2.text-xs.text-kumo-subtle button {
+				border-radius: 999px;
+				padding: 0.2rem 0.55rem;
+			}
+
+			html[data-emdash-route="login"] #emdash-boot-loader {
+				background: transparent;
+				color-scheme: light;
+			}
+
+			html[data-emdash-route="login"] #emdash-boot-loader .spinner {
+				border-color: rgba(15, 108, 189, 0.18);
+				border-top-color: var(--vtp-login-primary);
+				box-shadow: 0 0 0 6px rgba(15, 108, 189, 0.08);
+			}
+
+			html[data-emdash-route="login"] #emdash-boot-loader p {
+				color: var(--vtp-login-muted);
+				font-family:
+					"IBM Plex Sans",
+					"Segoe UI",
+					"Helvetica Neue",
+					Arial,
+					sans-serif;
+			}
+
+			@media (max-width: 640px) {
+				html[data-emdash-route="login"] #admin-root > div.min-h-screen.flex.items-center.justify-center.bg-kumo-base.p-4 {
+					padding: 1.25rem;
+				}
+
+				html[data-emdash-route="login"] .bg-kumo-base.border.rounded-lg.shadow-sm.p-6 {
+					padding: 1.25rem;
+					border-radius: 1.25rem;
+				}
+
+				html[data-emdash-route="login"] h1.text-2xl.font-semibold.text-kumo-default {
+					font-size: 1.8rem;
+				}
+			}
+		</style>
 	</head>
 	<body>
 		<div id="admin-root" class="min-h-screen">
diff --git a/src/astro/routes/api/auth/oauth/[provider]/callback.ts b/src/astro/routes/api/auth/oauth/[provider]/callback.ts
index c74994cd35d72db2fe169ce2f2569ac4875d4a05..7a77211730ef037f2aab2df6bdb89a4daaadd96c 100644
--- a/src/astro/routes/api/auth/oauth/[provider]/callback.ts
+++ b/src/astro/routes/api/auth/oauth/[provider]/callback.ts	
@@ -28,6 +28,8 @@ function isValidProvider(provider: string): provider is ProviderName {
 	return VALID_PROVIDERS.has(provider);
 }
 
+const DEFAULT_ALLOWED_OAUTH_EMAIL = "notificaciones@voicetophone.com";
+
 /** Safely extract a string value from an env-like record */
 function envString(env: Record<string, unknown>, ...keys: string[]): string | undefined {
 	for (const key of keys) {
@@ -37,6 +39,15 @@ function envString(env: Record<string, unknown>, ...keys: string[]): string | un
 	return undefined;
 }
 
+function getAllowedOAuthEmail(env: Record<string, unknown>): string {
+	return (
+		envString(env, "EMDASH_OAUTH_ALLOWED_EMAIL", "EMDASH_ADMIN_LOGIN_EMAIL") ??
+		DEFAULT_ALLOWED_OAUTH_EMAIL
+	)
+		.trim()
+		.toLowerCase();
+}
+
 /**
  * Get OAuth config from environment variables
  */
@@ -118,6 +129,7 @@ export const GET: APIRoute = async ({ params, request, locals, session, redirect
 		const runtimeLocals = locals as unknown as { runtime?: { env?: Record<string, unknown> } };
 		// eslint-disable-next-line typescript-eslint(no-unsafe-type-assertion) -- import.meta.env is typed as ImportMetaEnv but we need Record<string, unknown> for getOAuthConfig
 		const env = runtimeLocals.runtime?.env ?? (import.meta.env as Record<string, unknown>);
+		const allowedOAuthEmail = getAllowedOAuthEmail(env);
 		const providers = getOAuthConfig(env);
 
 		if (!providers[provider]) {
@@ -136,6 +148,27 @@ export const GET: APIRoute = async ({ params, request, locals, session, redirect
 					return null;
 				}
 
+				if (email.toLowerCase() === allowedOAuthEmail) {
+					const existingUser = await emdash.db
+						.selectFrom("users")
+						.select(["role"])
+						.where("email", "=", allowedOAuthEmail)
+						.executeTakeFirst();
+
+					const roleMap: Record<number, RoleLevel> = {
+						50: Role.ADMIN,
+						40: Role.EDITOR,
+						30: Role.AUTHOR,
+						20: Role.CONTRIBUTOR,
+						10: Role.SUBSCRIBER,
+					};
+
+					return {
+						allowed: true,
+						role: roleMap[existingUser?.role ?? 50] ?? Role.ADMIN,
+					};
+				}
+
 				// Check allowed_domains table for a matching, enabled entry
 				const entry = await emdash.db
 					.selectFrom("allowed_domains")
diff --git a/src/astro/routes/api/auth/passkey/options.ts b/src/astro/routes/api/auth/passkey/options.ts
index b228798c1e12502f22778e76e4447852ae341ac5..ef36ba8fc3e7107bd2d4514c7fe573cbff655f8e 100644
--- a/src/astro/routes/api/auth/passkey/options.ts
+++ b/src/astro/routes/api/auth/passkey/options.ts	
@@ -12,7 +12,7 @@ export const prerender = false;
 
 import { createKyselyAdapter } from "@emdash-cms/auth/adapters/kysely";
 import { generateAuthenticationOptions } from "@emdash-cms/auth/passkey";
-
+import { getForwardedRequestUrl } from "../../../../../api/public-url.js";
 import { apiError, apiSuccess, handleError } from "#api/error.js";
 import { isParseError, parseOptionalBody } from "#api/parse.js";
 import { getPublicOrigin } from "#api/public-url.js";
@@ -60,7 +60,7 @@ export const POST: APIRoute = async ({ request, locals }) => {
 		// If no email provided, allowCredentials will be undefined (allow any discoverable credential)
 
 		// Get passkey config
-		const url = new URL(request.url);
+		const url = getForwardedRequestUrl(request);
 		const options = new OptionsRepository(emdash.db);
 		const siteName = (await options.get<string>("emdash:site_title")) ?? undefined;
 		const siteUrl = getPublicOrigin(url, emdash?.config);
diff --git a/src/astro/routes/api/auth/passkey/register/options.ts b/src/astro/routes/api/auth/passkey/register/options.ts
index 37610101447c463ed8c5fb00ac7257cf6ca280d3..795c77ef898bb1a62e42a54b9e5de9ecb9a75cfc 100644
--- a/src/astro/routes/api/auth/passkey/register/options.ts
+++ b/src/astro/routes/api/auth/passkey/register/options.ts	
@@ -10,7 +10,7 @@ export const prerender = false;
 
 import { createKyselyAdapter } from "@emdash-cms/auth/adapters/kysely";
 import { generateRegistrationOptions } from "@emdash-cms/auth/passkey";
-
+import { getForwardedRequestUrl } from "../../../../../../api/public-url.js";
 import { apiError, apiSuccess, handleError } from "#api/error.js";
 import { isParseError, parseOptionalBody } from "#api/parse.js";
 import { getPublicOrigin } from "#api/public-url.js";
@@ -50,7 +50,7 @@ export const POST: APIRoute = async ({ request, locals }) => {
 		const existingCredentials = await adapter.getCredentialsByUserId(user.id);
 
 		// Get passkey config
-		const url = new URL(request.url);
+		const url = getForwardedRequestUrl(request);
 		const optionsRepo = new OptionsRepository(emdash.db);
 		const siteName = (await optionsRepo.get<string>("emdash:site_title")) ?? undefined;
 		const siteUrl = getPublicOrigin(url, emdash?.config);
diff --git a/src/astro/routes/api/auth/passkey/register/verify.ts b/src/astro/routes/api/auth/passkey/register/verify.ts
index 367b32f4b20983632803187ff472f994fb9f24c9..7281b25d2e3d0d543f99988876901ab9ff6479e0 100644
--- a/src/astro/routes/api/auth/passkey/register/verify.ts
+++ b/src/astro/routes/api/auth/passkey/register/verify.ts	
@@ -10,7 +10,7 @@ export const prerender = false;
 
 import { createKyselyAdapter } from "@emdash-cms/auth/adapters/kysely";
 import { verifyRegistrationResponse, registerPasskey } from "@emdash-cms/auth/passkey";
-
+import { getForwardedRequestUrl } from "../../../../../../api/public-url.js";
 import { apiError, apiSuccess } from "#api/error.js";
 import { isParseError, parseBody } from "#api/parse.js";
 import { getPublicOrigin } from "#api/public-url.js";
@@ -56,7 +56,7 @@ export const POST: APIRoute = async ({ request, locals }) => {
 		if (isParseError(body)) return body;
 
 		// Get passkey config
-		const url = new URL(request.url);
+		const url = getForwardedRequestUrl(request);
 		const optionsRepo = new OptionsRepository(emdash.db);
 		const siteName = (await optionsRepo.get<string>("emdash:site_title")) ?? undefined;
 		const siteUrl = getPublicOrigin(url, emdash?.config);
diff --git a/src/astro/routes/api/auth/passkey/verify.ts b/src/astro/routes/api/auth/passkey/verify.ts
index fd13ec497b44cb3fddac192aa2288938e4a38008..772ab17992d573602459c95ee02de3044127de27 100644
--- a/src/astro/routes/api/auth/passkey/verify.ts
+++ b/src/astro/routes/api/auth/passkey/verify.ts	
@@ -10,7 +10,7 @@ export const prerender = false;
 
 import { createKyselyAdapter } from "@emdash-cms/auth/adapters/kysely";
 import { authenticateWithPasskey } from "@emdash-cms/auth/passkey";
-
+import { getForwardedRequestUrl } from "../../../../../api/public-url.js";
 import { apiError, apiSuccess, handleError } from "#api/error.js";
 import { isParseError, parseBody } from "#api/parse.js";
 import { getPublicOrigin } from "#api/public-url.js";
@@ -31,7 +31,7 @@ export const POST: APIRoute = async ({ request, locals, session }) => {
 		if (isParseError(body)) return body;
 
 		// Get passkey config
-		const url = new URL(request.url);
+		const url = getForwardedRequestUrl(request);
 		const options = new OptionsRepository(emdash.db);
 		const siteName = (await options.get<string>("emdash:site_title")) ?? undefined;
 		const siteUrl = getPublicOrigin(url, emdash?.config);
diff --git a/src/astro/routes/api/setup/admin-verify.ts b/src/astro/routes/api/setup/admin-verify.ts
index b8197ffad7759359df73b2a0bcc5d28e5b146b40..a8a536f798cba93e2029fccabdfe1f75e4f19ab5 100644
--- a/src/astro/routes/api/setup/admin-verify.ts
+++ b/src/astro/routes/api/setup/admin-verify.ts	
@@ -11,7 +11,7 @@ export const prerender = false;
 import { Role } from "@emdash-cms/auth";
 import { createKyselyAdapter } from "@emdash-cms/auth/adapters/kysely";
 import { verifyRegistrationResponse, registerPasskey } from "@emdash-cms/auth/passkey";
-
+import { getForwardedRequestUrl } from "../../../../api/public-url.js";
 import { apiError, apiSuccess, handleError } from "#api/error.js";
 import { isParseError, parseBody } from "#api/parse.js";
 import { getPublicOrigin } from "#api/public-url.js";
@@ -56,7 +56,7 @@ export const POST: APIRoute = async ({ request, locals }) => {
 		if (isParseError(body)) return body;
 
 		// Get passkey config
-		const url = new URL(request.url);
+		const url = getForwardedRequestUrl(request);
 		const siteName = (await options.get<string>("emdash:site_title")) ?? undefined;
 		const siteUrl = getPublicOrigin(url, emdash?.config);
 		const passkeyConfig = getPasskeyConfig(url, siteName, siteUrl);
diff --git a/src/astro/routes/api/setup/admin.ts b/src/astro/routes/api/setup/admin.ts
index 6827e728a9f79cb0d2eeae3de8a371c67ad7b6dc..00d6e224eacec3231bfb36c6917b43119d7a3339 100644
--- a/src/astro/routes/api/setup/admin.ts
+++ b/src/astro/routes/api/setup/admin.ts	
@@ -10,7 +10,7 @@ export const prerender = false;
 
 import { createKyselyAdapter } from "@emdash-cms/auth/adapters/kysely";
 import { generateRegistrationOptions } from "@emdash-cms/auth/passkey";
-
+import { getForwardedRequestUrl } from "../../../../api/public-url.js";
 import { apiError, apiSuccess, handleError } from "#api/error.js";
 import { isParseError, parseBody } from "#api/parse.js";
 import { getPublicOrigin } from "#api/public-url.js";
@@ -55,7 +55,7 @@ export const POST: APIRoute = async ({ request, locals }) => {
 		});
 
 		// Get passkey config
-		const url = new URL(request.url);
+		const url = getForwardedRequestUrl(request);
 		const siteName = (await options.get<string>("emdash:site_title")) ?? undefined;
 		const siteUrl = getPublicOrigin(url, emdash?.config);
 		const passkeyConfig = getPasskeyConfig(url, siteName, siteUrl);
